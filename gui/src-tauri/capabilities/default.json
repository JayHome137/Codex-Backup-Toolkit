{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow CodexBackup desktop bridge commands.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
